let express,web,connectdb,join,cookieParser;_f0a‍.w("express",[["default",["express"],function(v){express=v}]]);_f0a‍.w("./routes/web.js",[["default",["web"],function(v){web=v}]]);_f0a‍.w("./db/connectdb",[["default",["connectdb"],function(v){connectdb=v}]]);_f0a‍.w("path",[["join",["join"],function(v){join=v}]]);_f0a‍.w("cookie-parser",[["default",["cookieParser"],function(v){cookieParser=v}]]);
const app = express();
const port = process.env.PORT || 3001;

// app.use('/', web);

//Database connection

const DATABASE_URL = process.env.DATABASE_URL || 'mongodb://localhost:27017';
connectdb(DATABASE_URL);

//static file


app.use('/student', web);
app.use('/',express.static(join(process.cwd(), 'public')));
app.use('/student',express.static(join(process.cwd(), 'public')));
app.use('/student/edit',express.static(join(process.cwd(), 'public')));

//Loading middleware(urlencoded) which is used to parse incomming requests
// app.use(express.urlencoded({ extended: false} ));

//Setting up engine
app.set('view engine', 'ejs');


app.listen(port, ()=>{
    _f0a‍.g.console.log("Listening to port",port);
})




app.use(express.json());
app.use(express.urlencoded({ extended: false }));
app.use(cookieParser());


// error handler
app.use(function(err, req, res, next) {
  // set locals, only providing error in development
  res.locals.message = err.message;
  res.locals.error = req.app.get('env') === 'development' ? err : {};
});
